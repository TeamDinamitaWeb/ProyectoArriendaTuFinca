apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tufinca
  namespace: default
spec:
  ingressClassName: public
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tufinca
                port:
                  number: 8081
